%\documentclass[../overview.tex]{subfiles}
\documentclass{standalone}
\usepackage{forest}
\useforestlibrary{edges}

\forestset{multiple directions/.style={for tree={#1}, phantom, for relative level=1{no edge, delay={!c.content/.pgfmath=content("!u")}, before computing xy={l=0,s=0}}},
    multiple directions/.default={},
    grow subtree/.style={for tree={grow=#1}}, 
    grow' subtree/.style={for tree={grow'=#1}}}

\begin{document}
    \begin{forest} 
        multiple directions={anchor=center}, forked edges
        [\acused{AD}\ac{AD} [, grow subtree=south
                [\textbf{\acused{Cluster}\ac{Cluster}}, grow subtree=east, calign=last [\acused{Density}\ac{Density} [\acused{LOF}\ac{LOF} ] [\acused{LSC}\ac{LSC} ] [\acused{MDEF}\ac{MDEF} ] [\acused{NegCo}\ac{NegCo} ] [\acused{DAAE}\ac{DAAE} ] ] [\acused{Distance}\ac{Distance} [\acused{DTW}\ac{DTW} ] [\acused{Euclid}\ac{Euclid} ] [\acused{DD - LCSS -DTW}\ac{DD - LCSS -DTW} ] [\acused{ERP}\ac{ERP} ] [ROCK ] ] [\acused{DBSCAN}\ac{DBSCAN} ] [\acused{GMM}\ac{GMM} [\acused{DAGMM}\ac{DAGMM} [\acused{PRDAGMM}\ac{PRDAGMM} ] ] [\acused{GMDNN}\ac{GMDNN} ] [\acused{LogLM}\ac{LogLM} ] ] [\acused{Bayesian}\ac{Bayesian} ] [SCAN ] [MapReduce ] [k-means++  ] [\acused{ELM-MI}\ac{ELM-MI} ] [\acused{MGM}\ac{MGM} ] ]
                [\textbf{\acused{Forcasting}\ac{Forcasting}}, grow subtree=east, calign=last [\acused{CNN}\ac{CNN} [\acused{AlexNet}\ac{AlexNet} ] [\acused{TCN}\ac{TCN} [HS-TCN ] [TCN-GMM ] [TCN-ms ] [TCN-AE ] ] [\acused{DCAD}\ac{DCAD} ] ] [\acused{RNN}\ac{RNN} [\acused{LSTM}\ac{LSTM} [LSTM-AE ] [LSTM-VAE ] [LSTM-NDT ] ] [\acused{GRU}\ac{GRU} [\acused{THOC}\ac{THOC} ] ] [LGMAD ] ] [\acused{Trfmr}\ac{Trfmr} [SAnD ] [MTSM ] [ECG ] ] [\acused{EKF}\ac{EKF} [\acused{ARIMA }\ac{ARIMA } [\acused{ARIMAX}\ac{ARIMAX} ] [\acused{SARIMA}\ac{SARIMA} ] ] ] [\acused{GAN}\ac{GAN} [BeatGAN ] [WGAN-based ] [RSM-GAN ] [CatGAN ] [\acused{DCGAN}\ac{DCGAN} ] ] [\acused{HTM}\ac{HTM} [HTM-based ] [RADM ] ] [\acused{GNN}\ac{GNN} [MTAD-GAT ] [AddGraph ] [MTAD-TF ] [\acused{CIA-GNN}\ac{CIA-GNN} ] ] [\acused{2D-Mx}\ac{2D-Mx} [\acused{PIP}\ac{PIP} ] [\acused{PP}\ac{PP} ] [\acused{MSE - L2}\ac{MSE - L2} ] [\acused{GAN-DM}\ac{GAN-DM} ] ] ]
                [\textbf{\acused{add-on}\ac{add-on}}, grow subtree=east, calign=last [\acused{Dim reduc}\ac{Dim reduc} [\acused{AE}\ac{AE} [\acused{SAE}\ac{SAE} ] [\acused{VAE}\ac{VAE} [GGM-VAE ] [OmniAnomaly ] [\acused{LUAD}\ac{LUAD} ] ] [SPEAD ] [S-RNNs ] [\acused{GSAE}\ac{GSAE} ] ] [\acused{SPREAD}\ac{SPREAD} ] [\acused{IU}\ac{IU} [ODCA ] ] [\acused{PCA}\ac{PCA} [\acused{R-PCA}\ac{R-PCA} ] [\acused{RPCA}\ac{RPCA} ] ] ] [\acused{Contamination}\ac{Contamination} [\acused{Core loss}\ac{Core loss} ] [\acused{LOE}\ac{LOE} ] [\acused{IAD}\ac{IAD} ] ] [\acused{Topologie}\ac{Topologie} [\acused{GDN}\ac{GDN} ] [\acused{GTA}\ac{GTA} ] ] ]
                [\textbf{\acused{Index}\ac{Index}}, grow subtree=east, calign=last [\acused{FT}\ac{FT} [\acused{STW}\ac{STW} ] [STFT ] [\acused{AR}\ac{AR} ] ] [MAD-GAN ] [\acused{Control chart}\ac{Control chart} ] ]
                ]]
    \end{forest}
\end{document}